// Imports
import { type BuildOptions } from 'vite';

// Type
type ViteEnv = ImportMetaEnv;

/**
 * Build
 */
export const createViteBuild = (env: ViteEnv, isBuild:boolean): BuildOptions => {
    return {};
}